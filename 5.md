# Session 5 — Classical Model and Notebook Alignment

**Date:** 2026-07-15
**Status:** Complete

## What I finished

- Audited the full project and saved the remaining work in
  `PROJECT_AUDIT_AND_ACTION_PLAN.md`.
- Replaced the old split with a 5,051/1,322 source-aware split. Rows connected by either
  `page_id` or generated-template skeleton cannot cross train/test or grouped CV folds.
- Tried the old advanced classical components under grouped validation instead of assuming they
  would help. Character TF-IDF remained much stronger than the legacy word/XGBoost path.
- Implemented the selected variant: character 2-6 grams, 12 engineered features, robust scaling,
  Yeo-Johnson, SMOTE, LinearSVC (`C=0.5`), and grouped sigmoid calibration.
- Updated the Streamlit inference path to use the exact 12 features and direct calibrated
  probabilities. Removed the old fallback threshold and confidence damping.
- Reduced `features.csv` and `ood_features.csv` to the exact 12-feature contract.
- Made all three notebooks self-contained and reran them. Notebook code no longer imports
  project scripts. Notebook 2 and Notebook 3 use the same embedded classical definition.
- Confirmed the label flow: `map_to_ccpa()` can produce 10 classes from the academic source; the
  supplementary data adds Bait and Switch, Nagging, Rogue Malware, and SaaS Billing, giving 13
  dark-pattern classes plus benign = 14 total.

## Latest metrics

All Notebook 3 rows below use the same page/template-grouped 5,051/1,322 split.

| Model | Test macro-F1 | Test accuracy | OOD-dev macro-F1 |
|---|---:|---:|---:|
| DistilBERT | 0.883 | 0.911 | 0.697 |
| Classical 12-feature model | 0.730 | 0.816 | 0.517 |
| LSTM from scratch | 0.657 | 0.784 | 0.389 |

The exported classical artifact's stored run is `0.739 ± 0.059` grouped CV, `0.732` test
macro-F1, `0.825` test accuracy, and `0.517` OOD-dev macro-F1.

## Checks completed

- 6,373 processed rows, 14 classes, and exactly 12 engineered features.
- Dataset hash matches the saved split manifest.
- Zero connected page/template overlap across the outer split.
- All notebooks validate, parse, and contain no saved error or warning outputs.
- Notebook 2 copies are byte-identical.
- Classical helper code in Notebooks 2 and 3 is identical.
- Unit tests, model inference, Streamlit startup, and `git diff --check` pass.

## Pick up next time

1. Replace legal verdict language with risk-screening/insufficient-context wording.
2. Review the HF Space confidence fallback and align it with the honest product framing.
3. Add model/data/split manifests and exact environment metadata.
4. Validate taxonomy/provenance and plan a real adjudicated benchmark.
5. Add CI and broader artifact/app regression checks.

The detailed evidence, priorities, and acceptance criteria are in
`PROJECT_AUDIT_AND_ACTION_PLAN.md`.
